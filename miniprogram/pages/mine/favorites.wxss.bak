.page { background: #1A1C29; color: #D1D5DB; }

/* 导航栏 */
.nav { height: 44px; display: flex; align-items: center; padding: 0 12px; background: rgba(0,0,0,0.3); }
.nav-left { width: 44px; font-size: 18px; }
.nav-title { flex: 1; text-align: center; font-size: 17px; font-weight: 600; }
.nav-right { width: 44px; }

/* Tab 栏 */
.tab-bar { display: flex; border-bottom: 1px solid rgba(255,255,255,0.1); }
.tab { 
  flex: 1; 
  padding: 12px; 
  text-align: center; 
  color: #9CA3AF; 
  font-size: 14px;
  position: relative;
}
.tab.active { color: #D1D5DB; }
.tab.active::after { 
  content: ''; 
  position: absolute; 
  bottom: 0; 
  left: 0; 
  right: 0; 
  height: 2px; 
  background: #3A9E8F;
}

/* 内容区 */
.content { padding: 12px 16px; }
.favorite-item { 
  padding: 12px;
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(58,158,143,0.2);
  border-radius: 8px;
  margin-bottom: 8px;
}
.item-title { font-size: 14px; color: #D1D5DB; font-weight: 600; margin-bottom: 4px; }
.item-meta { font-size: 12px; color: #9CA3AF; }

/* 空状态 */
.empty { text-align: center; padding: 40px 16px; }
.empty-text { color: #9CA3AF; font-size: 14px; }
