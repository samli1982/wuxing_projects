/**
 * 🎨 设计系统 V2 快速集成指南
 * 
 * 如何在现有页面和新页面中使用新的设计系统
 */

/* ========================================
   第一步：全局样式已更新
   ======================================== */

✅ app.wxss 已包含：
- 完整的色彩变量 (:root)
- 文字层级样式 (.text-title, .text-body, .text-secondary, .text-caption)
- 卡片基础样式 (.card)
- 按钮样式 (.btn-primary, .btn-secondary)
- 输入框样式 (.input-field)
- 分割线样式 (.divider)
- 淡出动画 (.fade-in-up)

所有页面会自动继承这些样式！

/* ========================================
   第二步：在页面 WXSS 中使用颜色变量
   ======================================== */

// ❌ 旧写法（不再使用）
.text { color: #BBBBBB; }        // 过时的灰色
.title { color: #FFFFFF; }       // 纯白不够温和
.card { border: 1px solid #BBB; } // 脏灰色

// ✅ 新写法（使用 V2 颜色系统）
.text { color: #9CA3AF; }        // 中灰 - 次要信息
.title { color: #D1D5DB; }       // 浅银灰 - 主标题
.card { 
  background: rgba(255, 255, 255, 0.06);
  border: 1px solid rgba(58, 158, 143, 0.15);
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

/* ========================================
   第三步：颜色对应关系速查表
   ======================================== */

[主正文]          → #D1D5DB (浅银灰)         // 页面、卡片内的主要文字
[次要文字]        → #9CA3AF (中灰)           // 时间、描述、辅助信息
[禁用/分割线]     → #6B7280 (淡雾灰)         // 禁用按钮、分割线、深层次
[高对比白字]      → #FFFFFF (纯白)           // 仅用于：按钮、强调标签、徽章
[强调/高亮]       → #3A9E8F (翠绿青)        // 按钮、链接、进度条、选中态
[背景色]          → #1A1C29 (深空蓝)        // 全局背景，保持一致
[卡片背景]        → rgba(255,255,255,0.06)  // 磨砂玻璃效果
[卡片边框]        → rgba(58,158,143,0.15)  // 浅绿边框

[五行色 - 木]     → #3A9E8F (翠绿)           // ✓ 与主色调一致，强调
[五行色 - 火]     → #E63946 (正红)           // ✓ 退出、危险操作
[五行色 - 土]     → #F4D06F (明黄)           // ✓ 警告、中性
[五行色 - 金]     → #FFFFFF (纯白)           // ✓ 重要、纯净
[五行色 - 水]     → #457B9D (深蓝)           // ✓ 信息、链接

/* ========================================
   第四步：常见组件样式模板
   ======================================== */

/* 按钮 */
.btn-primary {
  background: #3A9E8F;
  color: #FFFFFF;
  height: 52px;
  border-radius: 12px;
  font-weight: 600;
  box-shadow: 0 2px 8px rgba(58, 158, 143, 0.2);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
.btn-primary:active {
  transform: scale(0.98);
  box-shadow: 0 1px 4px rgba(58, 158, 143, 0.15);
}

/* 卡片 */
.card-item {
  background: rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(58, 158, 143, 0.15);
  border-radius: 12px;
  padding: 16px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

/* 输入框 */
.input-field {
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid rgba(58, 158, 143, 0.2);
  border-radius: 12px;
  color: #D1D5DB;
  height: 52px;
}
.input-field::placeholder {
  color: #9CA3AF;
}
.input-field:active {
  border-color: #3A9E8F;
  box-shadow: 0 0 0 2px rgba(58, 158, 143, 0.1);
}

/* 分割线（东方留白意境）*/
.divider {
  height: 1px;
  background: linear-gradient(to right,
    transparent,
    rgba(58, 158, 143, 0.3),
    transparent);
  margin: 16px 0;
}

/* 标签 */
.tag {
  display: inline-flex;
  align-items: center;
  padding: 4px 12px;
  background: rgba(58, 158, 143, 0.15);
  color: #3A9E8F;
  font-size: 12px;
  border-radius: 6px;
  border: 1px solid rgba(58, 158, 143, 0.3);
}

/* 进度条 */
.progress-bar {
  height: 4px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 2px;
  overflow: hidden;
}
.progress-fill {
  height: 100%;
  background: linear-gradient(to right, #3A9E8F, #2d7f71);
  transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

/* ========================================
   第五步：间距与排版规范（8pt 网格）
   ======================================== */

✅ 间距标准（都是 8 的倍数）：
- 8px   - 最小间距（图标间距）
- 12px  - 小间距（卡片间距）
- 16px  - 标准间距（卡片内边距、区域边距）
- 20px  - 中间距（大区域分隔）
- 24px  - 大间距（主区域分隔）
- 32px  - 超大间距（页面分隔）

✅ 文字行高：
- 标题：28px（字号18 + 行高28）
- 正文：24px（字号15 + 行高24）
- 辅助：20px（字号13 + 行高20）
- 标签：16px（字号12 + 行高16）

✅ 圆角标准：
- 按钮/卡片/输入框：12px
- 标签/徽章：6px
- 极小元素：4px

/* ========================================
   第六步：迁移清单（对现有页面的修改）
   ======================================== */

对每个页面的 WXSS 进行以下替换：

1. 文字色替换
   [查找] #FFFFFF      [替换] #D1D5DB  （主文字）
   [查找] #BBBBBB      [替换] #9CA3AF  （次文字）
   [查找] #E6E6E6      [替换] #D1D5DB  （标签文字）
   [查找] #ffffff      [替换] #D1D5DB  （保持一致性）

2. 卡片样式替换
   [旧] background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.1);
   [新] background: rgba(255, 255, 255, 0.06); border: 1px solid rgba(58, 158, 143, 0.15);

3. 边框色替换
   [查找] rgba(255,255,255,0.1)   [替换] rgba(58, 158, 143, 0.15)
   [查找] rgba(58,158,143,0.2)    [替换] rgba(58, 158, 143, 0.15)

4. 动效改进
   [旧] transition: all 0.3s;
   [新] transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);

5. 按钮样式改进
   [旧] transform: scale(0.95);
   [新] transform: scale(0.98); 加上 box-shadow 变化

/* ========================================
   第七步：新页面建议模板
   ======================================== */

/* 页面容器 */
.page {
  background: #1A1C29;
  color: #D1D5DB;
  padding-bottom: 20px;
}

/* 导航栏 */
.nav {
  height: 44px;
  display: flex;
  align-items: center;
  padding: 0 12px;
  background: rgba(0, 0, 0, 0.3);
}

.nav-title {
  flex: 1;
  text-align: center;
  color: #D1D5DB;
  font-size: 17px;
  font-weight: 600;
}

/* 内容区 */
.content {
  padding: 16px;
}

/* 卡片列表 */
.card-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.card-item {
  background: rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(58, 158, 143, 0.15);
  border-radius: 12px;
  padding: 16px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.card-title {
  font-size: 15px;
  color: #D1D5DB;
  font-weight: 600;
  margin-bottom: 8px;
}

.card-text {
  font-size: 13px;
  color: #9CA3AF;
  line-height: 1.6;
}

/* ========================================
   第八步：设计系统文件结构
   ======================================== */

miniprogram/
├── app.wxss                  ✅ 已更新 V2 全局样式
├── styles/
│   ├── design-system-v2.wxss  ✅ 新增 - 组件详细规范
│   └── DESIGN_SYSTEM_V2.txt   ✅ 新增 - 详细文档参考
├── pages/
│   ├── mine/
│   │   ├── index.wxss         ✅ 已更新 - 应用 V2 标准
│   │   └── ...
│   ├── palmtree/
│   │   ├── index.wxss         ⏳ 待更新
│   │   ├── result.wxss        ⏳ 待更新
│   │   └── ...
│   ├── learn/
│   │   ├── index.wxss         ⏳ 待更新
│   │   └── ...
│   ├── herb/
│   │   ├── index.wxss         ⏳ 待更新
│   │   └── ...
│   └── home/
│       ├── index.wxss         ⏳ 待更新
│       └── ...

/* ========================================
   第九步：常见问题 (FAQ)
   ======================================== */

Q: 我的页面已经有自定义样式，怎么办？
A: 优先使用 V2 颜色和尺寸，保持全局一致性。
   逐页迁移，不需要一次性全部改。

Q: 能否使用 CSS 变量？
A: 可以！在 app.wxss :root 中定义的变量在所有页面可用：
   color: var(--color-text-primary);

Q: 磨砂玻璃效果不支持怎么办？
A: 使用替代方案：
   background: rgba(26, 28, 41, 0.6);
   border: 1px solid rgba(58, 158, 143, 0.15);
   box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);

Q: 某页面的样式不是 V2 标准，用户会感到割裂吗？
A: 是的。建议列出所有页面，逐个迁移到 V2 标准，
   预计全站统一后用户体验会显著提升。

Q: 五行色应该怎么用？
A: 仅用于：
   ✅ 图表分类标识（如分布图）
   ✅ 标签、徽章、进度条
   ✅ 列表项前的色块
   ❌ 不用作主背景色
   ❌ 不用作主文字色

/* ========================================
   最后：核心信念
   ======================================== */

"这不是一个普通的健康APP，而是一本会呼吸的《天人之书》。"

设计原则：
- 深空意境：用深蓝背景营造宇宙感
- 科技禅意：用磨砂玻璃体现现代感与克制感
- 水墨美学：用渐变分割线体现东方留白
- 信息优先：用精准色彩层级引导视线
- 交互克制：所有动效都是 0.3s + cubic-bezier(0.4, 0, 0.2, 1)

记住：极简即是力量 ✨
