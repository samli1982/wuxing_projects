# 开发环境检查报告

**检查时间**: 2025-01-23  
**操作系统**: macOS (Darwin Kernel 23.6.0)  
**架构**: x86_64

---

## 📊 环境检查结果总览

| 工具 | 状态 | 版本 | 是否满足要求 |
|------|------|------|-------------|
| **Java (JDK)** | ❌ 未安装 | - | ❌ **需要 JDK 17+** |
| **Maven** | ❌ 未安装 | - | ❌ **需要 Maven 3.8+** |
| **Node.js** | ✅ 已安装 | v24.4.1 | ✅ 满足要求 |
| **NPM** | ✅ 已安装 | v11.4.2 | ✅ 满足要求 |
| **MySQL** | ✅ 已安装 | v9.4.0 | ✅ 满足要求 (需要8.0+) |
| **Redis** | ❌ 未安装 | - | ❌ **需要 Redis 6.0+** |
| **Git** | ✅ 已安装 | v2.15.0 | ⚠️ 建议升级到2.30+ |
| **Homebrew** | ✅ 已安装 | v4.6.9 | ✅ 可用于安装工具 |

---

## ⚠️ 需要安装的工具

### 1. Java JDK 17+ （必需）

**原因**: Spring Boot 3.2.0 需要 JDK 17 或更高版本

**安装方法**:

#### 方式一: 使用 Homebrew 安装 (推荐)
```bash
# 安装 OpenJDK 17
brew install openjdk@17

# 创建软链接
sudo ln -sfn /opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk-17.jdk

# 配置环境变量（添加到 ~/.zshrc）
echo 'export JAVA_HOME=$(/usr/libexec/java_home -v 17)' >> ~/.zshrc
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> ~/.zshrc

# 使配置生效
source ~/.zshrc

# 验证安装
java -version
```

#### 方式二: 下载安装包
- 访问 [Adoptium](https://adoptium.net/)
- 下载 JDK 17 for macOS
- 安装 .pkg 文件
- 配置 JAVA_HOME 环境变量

---

### 2. Maven 3.8+ （必需）

**原因**: 项目使用 Maven 进行构建和依赖管理

**安装方法**:

```bash
# 使用 Homebrew 安装
brew install maven

# 验证安装
mvn -version

# 配置国内镜像（可选，加速依赖下载）
# 编辑 ~/.m2/settings.xml
mkdir -p ~/.m2
cat > ~/.m2/settings.xml << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<settings>
  <mirrors>
    <mirror>
      <id>aliyun</id>
      <mirrorOf>central</mirrorOf>
      <name>Aliyun Maven</name>
      <url>https://maven.aliyun.com/repository/public</url>
    </mirror>
  </mirrors>
</settings>
EOF
```

---

### 3. Redis 6.0+ （必需）

**原因**: 项目使用 Redis 作为缓存

**安装方法**:

```bash
# 使用 Homebrew 安装
brew install redis

# 启动 Redis 服务
brew services start redis

# 或者手动启动
redis-server

# 验证安装
redis-cli ping
# 应该返回: PONG
```

**Redis 配置**:
- 默认端口: 6379
- 默认无密码
- 配置文件: /opt/homebrew/etc/redis.conf

---

## ✅ 已安装工具详情

### Node.js & NPM
```
Node.js: v24.4.1  ✅
NPM: v11.4.2      ✅
```
**状态**: 满足 Vue 3 前端开发需求

**用途**:
- Vue 3 前端开发
- 小程序开发工具
- 前端依赖管理

---

### MySQL
```
MySQL: v9.4.0  ✅
```
**状态**: 满足要求 (需要 8.0+)

**后续配置**:
1. 启动 MySQL 服务
```bash
# 查看 MySQL 服务状态
brew services list | grep mysql

# 启动 MySQL
brew services start mysql

# 或使用
mysql.server start
```

2. 创建数据库
```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE wuxing_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 导入表结构
mysql -u root -p wuxing_db < /Users/saml/GoProjects/src/wuxing_projects/backend/sql/schema.sql

# 导入初始数据
mysql -u root -p wuxing_db < /Users/saml/GoProjects/src/wuxing_projects/backend/sql/data.sql
```

---

### Git
```
Git: v2.15.0  ⚠️
```
**状态**: 可用，但建议升级

**升级方法**:
```bash
brew upgrade git
```

---

## 🚀 完整安装脚本

为了方便，这里提供一个一键安装所有缺失工具的脚本：

```bash
#!/bin/bash

echo "======================================"
echo "五行项目开发环境安装脚本"
echo "======================================"

# 1. 安装 JDK 17
echo ">>> 安装 JDK 17..."
brew install openjdk@17
sudo ln -sfn /opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk-17.jdk

# 2. 安装 Maven
echo ">>> 安装 Maven..."
brew install maven

# 3. 安装 Redis
echo ">>> 安装 Redis..."
brew install redis
brew services start redis

# 4. 升级 Git
echo ">>> 升级 Git..."
brew upgrade git

# 5. 配置环境变量
echo ">>> 配置环境变量..."
echo 'export JAVA_HOME=$(/usr/libexec/java_home -v 17)' >> ~/.zshrc
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> ~/.zshrc

# 6. 配置 Maven 镜像
echo ">>> 配置 Maven 镜像..."
mkdir -p ~/.m2
cat > ~/.m2/settings.xml << 'MAVEN_EOF'
<?xml version="1.0" encoding="UTF-8"?>
<settings>
  <mirrors>
    <mirror>
      <id>aliyun</id>
      <mirrorOf>central</mirrorOf>
      <name>Aliyun Maven</name>
      <url>https://maven.aliyun.com/repository/public</url>
    </mirror>
  </mirrors>
</settings>
MAVEN_EOF

echo "======================================"
echo "安装完成！请执行以下命令使环境变量生效："
echo "source ~/.zshrc"
echo "======================================"

# 7. 验证安装
echo ">>> 验证安装..."
source ~/.zshrc
java -version
mvn -version
redis-cli ping
git --version
```

**使用方法**:
```bash
# 保存上述脚本为 install.sh
chmod +x install.sh
./install.sh
```

---

## 📋 安装后验证清单

安装完成后，请依次执行以下命令验证：

```bash
# 1. 验证 Java
java -version
# 应该显示: openjdk version "17.x.x"

# 2. 验证 Maven
mvn -version
# 应该显示: Apache Maven 3.x.x

# 3. 验证 Redis
redis-cli ping
# 应该返回: PONG

# 4. 验证 MySQL
mysql --version
# 应该显示: mysql Ver 9.4.0

# 5. 验证 Node.js
node -v
# 应该显示: v24.4.1

# 6. 验证 NPM
npm -v
# 应该显示: v11.4.2
```

---

## 🎯 环境配置文件检查

### 1. 后端配置 (application-dev.yml)

需要修改以下配置：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wuxing_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: your_mysql_password  # ← 修改为你的 MySQL 密码
    
  data:
    redis:
      host: localhost
      port: 6379
      password:  # 如果设置了密码，在这里填写
```

### 2. 前端配置

检查 `frontend/package.json` 是否存在，如不存在需要初始化：

```bash
cd /Users/saml/GoProjects/src/wuxing_projects/frontend
npm install
```

---

## 🔧 IDE 推荐

### 后端开发
- **IntelliJ IDEA** (推荐)
  - 下载: https://www.jetbrains.com/idea/download/
  - 安装 Lombok 插件
  - 启用注解处理器

### 前端开发
- **VS Code** (推荐)
  - 下载: https://code.visualstudio.com/
  - 推荐插件:
    - Volar (Vue 3 支持)
    - ESLint
    - Prettier

### 小程序开发
- **微信开发者工具**
  - 下载: https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

---

## 📝 下一步操作

1. ✅ **安装缺失工具** (JDK 17、Maven、Redis)
2. ✅ **启动 MySQL 和 Redis 服务**
3. ✅ **创建数据库并导入数据**
4. ✅ **修改配置文件** (application-dev.yml)
5. ✅ **运行后端项目**: `cd backend && mvn spring-boot:run`
6. ✅ **运行前端项目**: `cd frontend && npm install && npm run dev`

---

## 🆘 常见问题

### Q1: brew 命令很慢？
**A**: 配置 Homebrew 国内镜像
```bash
export HOMEBREW_BREW_GIT_REMOTE="https://mirrors.tuna.tsinghua.edu.cn/git/homebrew/brew.git"
export HOMEBREW_CORE_GIT_REMOTE="https://mirrors.tuna.tsinghua.edu.cn/git/homebrew/homebrew-core.git"
brew update
```

### Q2: Maven 下载依赖很慢？
**A**: 已在安装脚本中配置了阿里云镜像

### Q3: MySQL 连接失败？
**A**: 检查 MySQL 是否启动: `brew services list | grep mysql`

### Q4: Redis 连接失败？
**A**: 检查 Redis 是否启动: `brew services list | grep redis`

---

**生成时间**: 2025-01-23  
**系统**: macOS (Darwin 23.6.0)
