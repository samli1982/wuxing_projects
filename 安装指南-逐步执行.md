# 开发环境安装指南 - 逐步执行版

## 🚀 方案选择

由于 Homebrew 下载速度慢，我们有两个方案：

---

## 方案一：使用国内镜像加速 Homebrew（推荐）

### 步骤 1：配置 Homebrew 使用清华大学镜像

在终端执行以下命令：

```bash
# 设置 Homebrew 镜像
export HOMEBREW_BOTTLE_DOMAIN=https://mirrors.tuna.tsinghua.edu.cn/homebrew-bottles
export HOMEBREW_API_DOMAIN=https://mirrors.tuna.tsinghua.edu.cn/homebrew-bottles/api
```

### 步骤 2：重新安装 JDK 17

```bash
brew install openjdk@17
```

这次应该会快很多！

---

## 方案二：直接下载安装包（最快）

### 步骤 1：下载 JDK 17

选择以下任一下载源：

**选项 A - Adoptium（推荐）**
1. 访问：https://adoptium.net/temurin/releases/
2. 选择：
   - Version: 17 (LTS)
   - Operating System: macOS
   - Architecture: x64
   - Package Type: JDK
3. 点击下载 `.pkg` 文件

**选项 B - 阿里云镜像（国内更快）**
```bash
# 直接下载
curl -O https://mirrors.aliyun.com/adoptium/17/jdk/x64/mac/OpenJDK17U-jdk_x64_mac_hotspot_17.0.9_9.pkg
```

### 步骤 2：安装下载的 .pkg 文件

双击下载的 `.pkg` 文件，按照提示完成安装。

### 步骤 3：验证安装

```bash
/usr/libexec/java_home -V
```

应该能看到 JDK 17 的路径。

### 步骤 4：配置环境变量

```bash
echo 'export JAVA_HOME=$(/usr/libexec/java_home -v 17)' >> ~/.zshrc
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

### 步骤 5：验证

```bash
java -version
```

应该显示：`openjdk version "17.x.x"`

---

## 安装完 JDK 后继续安装其他工具

### 安装 Maven

```bash
# 使用镜像加速
export HOMEBREW_BOTTLE_DOMAIN=https://mirrors.tuna.tsinghua.edu.cn/homebrew-bottles
brew install maven
```

### 安装 Redis

```bash
brew install redis
```

### 启动 Redis

```bash
brew services start redis
```

### 验证 Redis

```bash
redis-cli ping
# 应该返回：PONG
```

---

## 配置 Maven 国内镜像（加速依赖下载）

```bash
mkdir -p ~/.m2
cat > ~/.m2/settings.xml << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 
          http://maven.apache.org/xsd/settings-1.0.0.xsd">
  <mirrors>
    <mirror>
      <id>aliyun</id>
      <mirrorOf>central</mirrorOf>
      <name>Aliyun Maven</name>
      <url>https://maven.aliyun.com/repository/public</url>
    </mirror>
  </mirrors>
</settings>
EOF
```

---

## 最终验证

所有工具安装完成后，执行：

```bash
java -version
mvn -version
redis-cli ping
node -v
npm -v
mysql --version
```

---

## 建议

**推荐使用方案二（直接下载安装包）**，原因：
1. ✅ 速度最快
2. ✅ 不依赖 Homebrew 网络
3. ✅ 安装更稳定

安装完成后告诉我，我会继续指导您配置数据库和启动项目！
