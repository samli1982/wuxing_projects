# 干支纪年历 - 快速开始指南

## ✨ 你将获得什么

使用 **tyme4j** 库实现的完整干支纪年历系统。现在可以轻松获取：

- 🎋 干支年月日时 (乙巳年 丁亥月 戊戌日)
- 🌾 纳音五行 (平地木、屋上土等)
- 🔮 建除十二值神 (建、除、满、平、定、执、破、危、成、收、开、闭)
- ✨ 黄道黑道十二神 (吉凶判断)
- 🌟 二十八星宿 (室火猪、壁水獒等)
- 📅 农历日期和生肖
- 🌤️ 节气和养生建议

## 🎯 3分钟快速上手

### 步骤 1: 理解核心类

**最重要的类: `SixtyCycleDay`** ⭐

```java
import java.time.LocalDate;
import com.tyme.solar.SolarDay;
import com.tyme.sixtycycle.SixtyCycleDay;

// 创建公历日期
LocalDate date = LocalDate.of(2025, 11, 25);
SolarDay solarDay = SolarDay.fromYmd(2025, 11, 25);

// 创建干支日期 - 自动计算所有信息！
SixtyCycleDay sixtyCycleDay = new SixtyCycleDay(solarDay);

// 获取各项信息
String year = sixtyCycleDay.getYear().getName();        // 乙巳
String month = sixtyCycleDay.getMonth().getName();      // 丁亥
String day = sixtyCycleDay.getSixtyCycle().getName();   // 戊戌
```

### 步骤 2: 使用项目的工具类

项目已经帮你封装好了！直接用就行：

```java
import java.time.LocalDate;
import com.wuxing.utils.TymeCalendarUtils;

LocalDate date = LocalDate.of(2025, 11, 25);

// 一句话获取所有信息
TymeCalendarUtils.SixtyCycleDayInfo dayInfo = 
    TymeCalendarUtils.getSixtyCycleDayInfo(date);

// 使用信息
System.out.println("干支日: " + dayInfo.day);        // 戊戌
System.out.println("纳音: " + dayInfo.daySound);      // 平地木
System.out.println("值神: " + dayInfo.duty);          // 闭
System.out.println("十二神: " + dayInfo.twelveStar); // 吉
System.out.println("星宿: " + dayInfo.twentyEightStar); // 室火猪
```

### 步骤 3: 在你的代码中使用

在 Service 或 Controller 中调用：

```java
@Service
public class MyService {
    
    public void showCalendarInfo(LocalDate date) {
        // 直接调用
        TymeCalendarUtils.SixtyCycleDayInfo dayInfo = 
            TymeCalendarUtils.getSixtyCycleDayInfo(date);
        
        // 使用返回的信息
        System.out.println("干支: " + dayInfo.year + " " + 
                           dayInfo.month + " " + dayInfo.day);
        System.out.println("吉凶: " + dayInfo.twelveStar);
    }
}
```

## 📚 完整示例代码

### 例1: 获取完整干支信息

```java
import java.time.LocalDate;
import com.tyme.solar.SolarDay;
import com.tyme.sixtycycle.SixtyCycleDay;
import com.tyme.culture.Zodiac;

LocalDate date = LocalDate.of(2025, 11, 25);

// 方法1: 直接使用 tyme4j
SolarDay solarDay = SolarDay.fromYmd(2025, 11, 25);
SixtyCycleDay scday = new SixtyCycleDay(solarDay);

String year = scday.getYear().getName();              // 乙巳
String month = scday.getMonth().getName();            // 丁亥
String day = scday.getSixtyCycle().getName();         // 戊戌
String sound = scday.getSixtyCycle().getSound().getName(); // 平地木
String duty = scday.getDuty().getName();              // 闭
String star = scday.getTwelveStar().getName();        // 吉
String star28 = scday.getTwentyEightStar().getName(); // 室火猪

Zodiac zodiac = scday.getYear().getEarthBranch().getZodiac();
String animal = zodiac.getName(); // 蛇

System.out.println(year + month + day);
System.out.println("纳音: " + sound);
System.out.println("值神: " + duty);
System.out.println("吉凶: " + star);
System.out.println("星宿: " + star28);
System.out.println("生肖: " + animal);
```

### 例2: 获取时辰信息和纳音

```java
import java.time.LocalDateTime;
import com.tyme.solar.SolarTime;
import com.tyme.sixtycycle.SixtyCycleHour;
import com.tyme.sixtycycle.SixtyCycle;

LocalDateTime dateTime = LocalDateTime.of(2025, 11, 25, 14, 30, 0);

SolarTime solarTime = SolarTime.fromYmdHms(2025, 11, 25, 14, 30, 0);
SixtyCycleHour scHour = solarTime.getSixtyCycleHour();

SixtyCycle year = scHour.getYear();
SixtyCycle month = scHour.getMonth();
SixtyCycle day = scHour.getDay();
SixtyCycle hour = scHour.getSixtyCycle();

System.out.println("年: " + year.getName() + "(" + year.getSound().getName() + ")");
System.out.println("月: " + month.getName() + "(" + month.getSound().getName() + ")");
System.out.println("日: " + day.getName() + "(" + day.getSound().getName() + ")");
System.out.println("时: " + hour.getName() + "(" + hour.getSound().getName() + ")");

// 输出:
// 年: 乙巳(平地木)
// 月: 丁亥(屋上土)
// 日: 戊戌(平地木)
// 时: 未时(天河水)
```

### 例3: 获取农历信息

```java
import java.time.LocalDate;
import com.tyme.solar.SolarDay;
import com.tyme.lunar.LunarDay;

LocalDate date = LocalDate.of(2025, 11, 25);

SolarDay solarDay = SolarDay.fromYmd(2025, 11, 25);
LunarDay lunarDay = solarDay.getLunarDay();

String lunarMonth = lunarDay.getLunarMonth().getName(); // 十月
String lunarDate = lunarDay.getName();                  // 廿五

System.out.println("农历: " + lunarMonth + lunarDate); // 十月廿五
```

## 🔗 三种使用方式

### 方式1: 直接使用 tyme4j (最灵活)
```java
// 优点: 直接使用 tyme4j 的所有功能
// 缺点: 代码稍多
SolarDay solarDay = SolarDay.fromYmd(2025, 11, 25);
SixtyCycleDay scday = new SixtyCycleDay(solarDay);
String day = scday.getSixtyCycle().getName();
```

### 方式2: 使用项目工具类 (推荐)
```java
// 优点: 简洁、封装好
// 缺点: 功能有限制
TymeCalendarUtils.SixtyCycleDayInfo dayInfo = 
    TymeCalendarUtils.getSixtyCycleDayInfo(date);
String day = dayInfo.day;
```

### 方式3: 通过服务类 (最简单)
```java
// 优点: 直接使用、无需关心细节
// 缺点: 需要注入 Service
emperorCalendarService.getSixtyCycleDayInfo(date);
```

## 💪 高级功能

### 获取纳音五行
```java
SixtyCycle sc = sixtyCycleDay.getSixtyCycle();
String sound = sc.getSound().getName(); // 平地木
```

### 判断吉凶
```java
String inauspicious = sixtyCycleDay.getTwelveStar().getName();
boolean isAuspicious = inauspicious.contains("吉");
```

### 获取生肖
```java
Zodiac zodiac = sixtyCycleDay.getYear()
    .getEarthBranch()
    .getZodiac();
String animal = zodiac.getName();
```

### 获取节气
```java
SolarDay solarDay = SolarDay.fromYmd(2025, 11, 25);
SolarTerm term = solarDay.getTerm();
String termName = term.getName(); // 小雪、大雪等
```

## 🎓 关键概念

### 干支 (Sexagenary Cycle)
- **周期**: 60年一轮回
- **组成**: 10个天干 + 12个地支
- **应用**: 年、月、日、时

### 纳音五行 (Sound)
- **定义**: 根据干支推算的五行属性
- **对应关系**: 按特定公式循环
- **应用**: 命理分析、五行相克

### 值神 (Duty)
- **定义**: 建除十二值神
- **周期**: 12天一轮回
- **应用**: 宜忌判断

### 星宿 (Star)
- **黄道黑道十二神**: 12种
- **二十八星宿**: 28种（古代天文）
- **应用**: 方位和吉凶

## ⚠️ 注意事项

1. **日期范围**: tyme4j 支持公元前100年至9999年
2. **时区**: 使用系统时区，如需特定时区请转换
3. **精度**: 农历至日，节气至时
4. **缓存**: 复频调用建议添加缓存

## 🚀 一键编译验证

```bash
cd /Users/saml/GoProjects/src/wuxing_projects/backend
mvn clean compile -DskipTests
```

**预期结果**: ✅ BUILD SUCCESS

## 📖 相关文档

| 文档 | 内容 | 用途 |
|-----|------|------|
| `干支纪年历项目概览.md` | 项目整体说明 | 了解项目 |
| `干支纪年历_tyme4j_快速参考.md` | API快速查阅 | 快速查找 |
| `干支纪年历_tyme4j_引用说明.md` | 详细实现说明 | 深入学习 |
| `干支纪年历实现_完成总结.md` | 完成总结报告 | 了解进展 |

## ✅ 核实清单

使用前请确认:
- [x] 项目已编译成功
- [x] tyme4j 库已导入
- [x] TymeCalendarUtils 已在 classpath 中
- [x] LocalDate/LocalDateTime 导入正确

## 🎉 成功标志

当你能运行以下代码并获得输出时，说明集成成功：

```java
import java.time.LocalDate;
import com.wuxing.utils.TymeCalendarUtils;

LocalDate date = LocalDate.of(2025, 11, 25);
TymeCalendarUtils.SixtyCycleDayInfo dayInfo = 
    TymeCalendarUtils.getSixtyCycleDayInfo(date);

System.out.println("✅ 干支日: " + dayInfo.day);
System.out.println("✅ 纳音: " + dayInfo.daySound);
System.out.println("✅ 值神: " + dayInfo.duty);
System.out.println("✅ 十二神: " + dayInfo.twelveStar);
System.out.println("✅ 星宿: " + dayInfo.twentyEightStar);

// 预期输出:
// ✅ 干支日: 戊戌
// ✅ 纳音: 平地木
// ✅ 值神: 闭
// ✅ 十二神: 吉
// ✅ 星宿: 室火猪
```

## 📞 需要帮助?

- 查看快速参考: `干支纪年历_tyme4j_快速参考.md`
- 查看详细说明: `干支纪年历_tyme4j_引用说明.md`
- 查看示例代码: 本文件的"完整示例代码"部分

---

**现在开始使用吧！** 🚀

当前日期: 2025-11-25 星期二 乙巳年 丁亥月 戊戌日
